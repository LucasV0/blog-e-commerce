<form method="post" action="{{ path('app_product_admin_delete', {'id': product.id}) }}" onsubmit="return confirm('Etes-vous sur de vouloir supprimer cet élément ?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ product.id) }}">
    <button class="btn">Delete</button>
</form>
